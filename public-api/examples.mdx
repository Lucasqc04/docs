---
title: Exemplos de Uso
---

## Teste rápido

```bash
# Criar pagamento
curl -s -X POST "https://<host>/api-public/payments" \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "amount": 150.0,
    "email": "cliente@example.com",
    "observation": "Pedido #9876",
    "webhookUrl": "https://example.com/webhook"
  }'

# Buscar por ID
curl -s -X GET "https://<host>/api-public/payments/pay_123" \
  -H "Authorization: Bearer <TOKEN>"

# Listar
curl -s -X GET "https://<host>/api-public/payments?page=1&limit=50&status=approved" \
  -H "Authorization: Bearer <TOKEN>"
```
